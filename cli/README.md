# SpectraOps CLI

The SpectraOps CLI provides commands for interacting with the SpectraOps platform from your terminal and CI/CD pipelines.

## Usage

```sh
pnpm dev --filter ./cli -- hello
```

## TODO

- Add project initialization
- Add commands for error tracking, visual regression, feature flags, and QA
- Integrate with the core engine API
